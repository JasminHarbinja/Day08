// Only change code below this line
function isLess(a, b) {
    // Only change code below this line
    if (a < b) {
        // Only change code below this line
        return true;
        // Only change code below this line

    } else {
        // Only change code below this line
        return false;
        // Only change code below this line
    }
}
cosnsole.log(isLess(10, 15));
cosnsole.log(isLess(15, 10));

module.exports = isLess;